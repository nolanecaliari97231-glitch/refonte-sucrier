# Generate a bcrypt/apache hash, then replace this line:
# You can generate with:
#   htpasswd -nbB staging_user "VeryStrongPassword"
# or use an online Apache htpasswd generator (bcrypt mode).

staging_user:$2y$05$REPLACE_WITH_REAL_BCRYPT_HASH
